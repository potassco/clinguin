skill(root).
skill(programming).
skill(logic).
skill(python).
skill(prolog).
skill(asp).

parent(programming, root).
parent(logic, root).
parent(python, programming).
parent(prolog, logic).
parent(asp, prolog).
